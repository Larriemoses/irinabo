import Link from"next/link";import{Brand}from"@/components/Brand";import{StatusPill}from"@/components/StatusPill";import{capabilities}from"@/lib/demo/data";
export default function Judge(){const tone={LIVE:'green',SIMULATED:'orange',FICTIONAL:'lime',DEFERRED:'grey'}as const;return <main className="min-h-screen bg-[var(--paper)]"><nav className="mx-auto flex max-w-6xl items-center justify-between px-6 py-6"><Brand/><Link href="/dashboard" className="rounded-full bg-[var(--forest)] px-5 py-3 text-sm font-bold text-white">Open dashboard</Link></nav><section className="mx-auto max-w-6xl px-6 py-12"><p className="text-xs font-bold uppercase tracking-[.2em] text-[var(--leaf)]">Read-only evidence view</p><h1 className="display mt-3 max-w-3xl text-6xl leading-none text-[var(--forest)]">What is real in this demonstration?</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-[var(--muted)]">Judge Mode reports capability status. It does not enable integrations, bypass authentication or weaken access controls.</p><div className="mt-10 overflow-hidden rounded-3xl border border-[var(--line)] bg-white">{capabilities.map((c,i)=><div key={c.name} className={`grid gap-3 p-6 sm:grid-cols-[180px_1fr] ${i?'border-t border-[var(--line)]':''}`}><StatusPill tone={tone[c.state]}>{c.state}</StatusPill><div><h2 className="font-bold">{c.name}</h2><p className="mt-1 text-sm leading-6 text-[var(--muted)]">{c.note}</p></div></div>)}</div><div className="mt-8 grid gap-5 md:grid-cols-3">{[['Routing tests','Protected and ordinary access boundaries'],['State tests','Acknowledgement and review transitions'],['Build evidence','Factual entries linked to verification']].map(([title,copy])=><div key={title} className="rounded-2xl bg-[var(--forest)] p-6 text-white"><p className="text-xs uppercase tracking-widest text-[var(--lime)]">Evidence</p><h3 className="display mt-3 text-2xl">{title}</h3><p className="mt-2 text-sm text-white/60">{copy}</p></div>)}</div></section></main>}
