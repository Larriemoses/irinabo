import{NextResponse}from"next/server";export async function POST(request:Request){if(!process.env.INTERNAL_CRON_SECRET||request.headers.get('authorization')!==`Bearer ${process.env.INTERNAL_CRON_SECRET}`)return NextResponse.json({error:'Unauthorized'},{status:401});return NextResponse.json({claimed:1,result:'SIMULATED',route:'PROTECTED',target:'independent-safety-demo',accepted:false,note:'No external call was placed.'})}
