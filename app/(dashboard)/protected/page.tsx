import Link from "next/link";import {StatusPill} from "@/components/StatusPill";import {incidents} from "@/lib/demo/data";
export default function Protected(){const list=incidents.filter(x=>x.routingClass==='PROTECTED');return <main className="p-5 lg:p-8"><p className="text-sm text-[var(--muted)]">Safety-role-only queue</p><h1 className="display mt-1 text-4xl text-[var(--forest)]">Protected inbox</h1><div className="mt-5 rounded-2xl border border-orange-200 bg-orange-50 p-4 text-sm leading-6"><strong>Restricted access.</strong> Opening a protected record creates an access event. Reporter identity and precise location are separately permissioned.</div><div className="mt-7 grid gap-4">{list.map(x=><Link key={x.id} href={`/incidents/${x.id}`} className="rounded-3xl border border-black/5 bg-white p-6 transition hover:-translate-y-0.5 hover:shadow-lg"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-widest text-[var(--orange)]">Immediate help requested</p><h2 className="display mt-2 text-2xl">{x.title}</h2><p className="mt-2 text-sm text-[var(--muted)]">{x.reference} · Journey TW204 · Identity protected</p></div><StatusPill tone="orange">AWAITING OWNER</StatusPill></div><div className="mt-5 flex flex-wrap gap-5 border-t border-[var(--line)] pt-5 text-sm"><span><b>Route:</b> Safety coordinator</span><span><b>Deadline:</b> passed</span><span><b>Fallback:</b> Independent safety contact</span></div></Link>)}</div></main>}
