import { requireStaff } from "@/lib/auth/staff";
import { logout } from "@/app/(auth)/login/actions";

export default async function SettingsPage() {
  const staff = await requireStaff();
  return <main className="max-w-3xl p-5 lg:p-8"><p className="text-xs font-bold uppercase tracking-widest text-[var(--leaf)]">Account</p><h1 className="display mt-2 text-4xl text-[var(--forest)]">Settings</h1><p className="mt-3 text-[var(--muted)]">Manage your account and sign-in details.</p><section className="mt-8 rounded-3xl border border-black/5 bg-white p-6"><h2 className="display text-2xl">Your profile</h2><dl className="mt-5 grid gap-4 sm:grid-cols-2"><div><dt className="text-xs font-bold uppercase tracking-widest text-[var(--muted)]">Name</dt><dd className="mt-1 font-semibold">{staff.displayName}</dd></div><div><dt className="text-xs font-bold uppercase tracking-widest text-[var(--muted)]">Email</dt><dd className="mt-1 font-semibold">{staff.email}</dd></div><div><dt className="text-xs font-bold uppercase tracking-widest text-[var(--muted)]">Company</dt><dd className="mt-1 font-semibold">{staff.organisationName}</dd></div><div><dt className="text-xs font-bold uppercase tracking-widest text-[var(--muted)]">Access</dt><dd className="mt-1 font-semibold">{staff.role.replaceAll("_", " ")}</dd></div></dl></section><section className="mt-5 rounded-3xl border border-black/5 bg-white p-6"><h2 className="display text-2xl">Security</h2><p className="mt-3 text-sm leading-6 text-[var(--muted)]">Your session is refreshed securely by Supabase. Sign out when you finish using a shared computer. Automatic inactivity timeout will be enabled before production launch.</p><form action={logout} className="mt-5"><button className="rounded-full bg-[var(--forest)] px-5 py-3 text-sm font-bold text-white">Sign out</button></form></section></main>;
}
