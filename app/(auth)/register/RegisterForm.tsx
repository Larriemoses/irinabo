"use client";
import { useActionState } from "react";
import { register } from "./actions";
export function RegisterForm(){const[state,action,pending]=useActionState(register,undefined);const field="w-full rounded-xl border border-[var(--line)] bg-black/20 px-4 py-3 text-white placeholder:text-white/45 outline-none focus:border-[var(--lime)]";return <form action={action} className="mt-8 space-y-4"><input name="company" required placeholder="Company name" className={field}/><input name="name" required placeholder="Your full name" className={field}/><input name="email" required type="email" placeholder="Work email" className={field}/><input name="password" required minLength={8} type="password" placeholder="Password (8+ characters)" className={field}/><input name="tripCode" required placeholder="First trip code, e.g. LAG-001" className={`${field} uppercase`}/>{state?.error&&<p role="alert" className="rounded-xl bg-red-50 px-4 py-3 text-sm font-semibold text-red-800">{state.error}</p>}<button disabled={pending} className="w-full rounded-full bg-[var(--lime)] px-5 py-3 font-bold text-[var(--forest)] disabled:opacity-60">{pending?"Creating company…":"Create company account"}</button></form>}
