"use client";import{useState}from"react";
export function Receipt(){const[state,setState]=useState<'open'|'matches'|'review'>('open');return <div className="paper-shadow w-full max-w-xl rounded-[32px] bg-white p-7 sm:p-9"><p className="text-xs font-bold uppercase tracking-widest text-[var(--leaf)]">Resolution receipt · IRN-204-031</p><h1 className="display mt-2 text-4xl">Your report&apos;s follow-up</h1><p className="mt-3 text-sm text-[var(--muted)]">This receipt shows recorded actions. It does not prove that an outcome occurred exactly as recorded.</p><div className="mt-7 space-y-0">{[['11:42','Report received','Your original report was stored.'],['11:42','Safety route notified','Notification simulated for this proof of concept.'],['11:46','Responsibility accepted','Independent safety contact · simulated acceptance'],['12:05','Action recorded','Operator instructed to preserve the stop and send a replacement vehicle.'],['12:28','Closed with outcome','Replacement vehicle recorded as arriving.']].map(([time,title,detail],i)=><div className="flex gap-4" key={title}><div className="flex flex-col items-center"><span className="grid size-7 place-items-center rounded-full bg-[var(--lime)] text-xs font-bold">✓</span>{i<4&&<span className="h-14 w-px bg-[var(--line)]"/>}</div><div><p className="text-xs text-[var(--muted)]">{time}</p><p className="font-bold">{title}</p><p className="text-sm text-[var(--muted)]">{detail}</p></div></div>)}</div>{state==='open'?<div className="mt-8 border-t border-[var(--line)] pt-6"><p className="font-bold">Does this outcome match what happened?</p><div className="mt-4 grid gap-3 sm:grid-cols-2"><button onClick={()=>setState('matches')} className="rounded-full border border-[var(--line)] px-5 py-3 font-bold">Yes, it matches</button><button onClick={()=>setState('review')} className="rounded-full bg-[var(--orange)] px-5 py-3 font-bold text-white">This is not resolved</button></div></div>:<div className={`mt-8 rounded-2xl p-5 ${state==='review'?'bg-orange-50':'bg-emerald-50'}`}><strong>{state==='review'?'Review requested':'Feedback recorded'}</strong><p className="mt-1 text-sm text-[var(--muted)]">{state==='review'?'Your response was appended to the timeline. The earlier closure record was not deleted or rewritten.':'Your confirmation was appended to the incident history.'}</p></div>}</div>}
