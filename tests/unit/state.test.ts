import{describe,expect,it}from"vitest";import{transitionResponse}from"@/lib/incidents/state-machine";import{locationFreshness,haversineMetres}from"@/lib/location/freshness";
describe('accountability state',()=>{it('requires explicit acceptance',()=>{expect(transitionResponse('AWAITING_ACKNOWLEDGEMENT','ACCEPTED')).toBe('ACCEPTED');expect(()=>transitionResponse('AWAITING_ACKNOWLEDGEMENT','ACTION_UNDERWAY')).toThrow()});it('appends review after closure',()=>expect(transitionResponse('CLOSED_WITH_OUTCOME','REVIEW_REQUESTED')).toBe('REVIEW_REQUESTED'))});
describe('location evidence',()=>{const now=new Date('2026-09-16T12:00:00Z');it('labels freshness honestly',()=>{expect(locationFreshness('2026-09-16T11:59:20Z',now)).toBe('ACTIVE');expect(locationFreshness('2026-09-16T11:57:00Z',now)).toBe('DELAYED');expect(locationFreshness('2026-09-16T11:50:00Z',now)).toBe('STALE')});it('measures source separation',()=>expect(haversineMetres({latitude:6.67,longitude:3.63},{latitude:6.68,longitude:3.63})).toBeGreaterThan(1000))});
